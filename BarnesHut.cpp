#include <vector>
#include <cmath>
#include <TreeNode>

class BarnesHut{
    private:
        TreeNode* root;

    public:
        BarnesHut(TreeNode node){
            root = node
        }

        void insert(TreeNode node){
            root.insertBody(node);
        }
}